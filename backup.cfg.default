#!/bin/bash


# If you need to add a special private key for git:
# echo -e "Loading ssh keys ..."
# eval "$(ssh-agent -s)"
# ssh-add "/path/to/private/key" 2>ssh-add.log

echo -e "Loading Config file ..."

BACKUP_DESTINATION="../"
USERS_TO_BACKUP=(
	Elanis
)
ORGS_TO_BACKUP=(
	L-U-M-E-N
	Dysnomia-Studio
)
USER_PAT="USERNAME:PAT" # Get personal access token with read:user and repo
